# App icon badge removal status

Status: completed

## Delivered

- Use `assets/icon.png` and `assets/adaptive-icon.png` in every environment.
- Remove badge generation from install, prebuild, iOS, and Android workflows.
- Remove `app-icon-badge`, its pnpm patch, generator, and obsolete test.
- Add regression coverage for both static Expo icon paths.
- Align current setup documentation with shared icon assets.

## Root cause

Non-production Expo config selected versioned images generated by
`app-icon-badge`; development builds therefore embedded `DEVELOPMENT` and the
app version into launcher artwork.

## Evidence

- Development, staging, and production Expo config resolve both source icons.
- Clean development prebuild and iOS build passed.
- Installed iOS Home Screen icon visually has no environment/version badge.
- Jest: 2 suites, 2 tests passed.
- TypeScript and focused ESLint passed.
- Frozen offline install and `git diff --check` passed.
- Whole-repository lint has two unrelated existing class-order errors.
- Independent review: no findings.

## Unresolved questions

- None.
